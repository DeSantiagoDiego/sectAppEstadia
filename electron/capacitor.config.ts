import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.sectapp',
  appName: 'sectapp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
